package views.main.graph_helper;

public interface GraphDataInjector {
    /*
    STEP 4
        Graph is built, vertices and edge structural connections are calculated, now data needs to be injected.
        User can later display the injected data.
     */
    public void injectGraphData();
}
